apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: mysql-primary-network-partition
  namespace: chaos-mesh
spec:
  action: partition
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "mysql-ha-cluster"
      "kubedb.com/role": "primary"
  target:
    mode: all
    selector:
      namespaces:
        - demo
      labelSelectors:
#        "app.kubernetes.io/instance": "mysql-ha-cluster"
        "kubedb.com/role": "standby"
#        "statefulset.kubernetes.io/pod-name": "mysql-ha-cluster-0"
  direction: both
  duration: "2m"
