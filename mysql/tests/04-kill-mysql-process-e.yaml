apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: mysql-kill-process-fixed-percent
  namespace: chaos-mesh
spec:
  action: container-kill
  mode: fixed-percent
  value: "66"
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "mysql-ha-cluster"
  containerNames:
    - mysql
  duration: "30s"