apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata:
  name: mysql-io-latency-all
  namespace: chaos-mesh
spec:
  action: latency
  mode: all
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "mysql-ha-cluster"
  volumePath: /var/lib/mysql
  path: /var/lib/mysql/**/*
  delay: "200ms"
  percent: 100
  duration: "3m"
  containerNames:
    - mysql